[agent]
  interval = "10s"
  round_interval = true
  omit_hostname = false

# Minimal input: Telegraf's own internal stats
[[inputs.internal]]
  collect_memstats = true

# Simple test script
# [[inputs.exec]]
#   commands = ["/app/scripts/test_simple.py"]
#   timeout = "15s"
#   interval = "10s"
#   data_format = "influx"

# UniFi devices script
# [[inputs.exec]]
#   commands = ["/app/scripts/unifi_devices.py"]
#   timeout = "15s"
#   interval = "60s"
#   data_format = "influx" # Tells Telegraf the script outputs InfluxDB line protocol format


# Output to InfluxDB v3
[[outputs.influxdb_v2]]
  urls = ["http://influxdb:8181"]
  token = "${INFLUXDB_TOKEN}"
  organization = ""
  bucket = "atlas"